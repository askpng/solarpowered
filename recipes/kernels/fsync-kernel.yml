modules:
  - type: rpm-ostree
    replace:
      # - from-repo: https://copr.fedorainfracloud.org/coprs/sentry/kernel-fsync/repo/fedora-%OS_VERSION%/sentry-kernel-fsync-fedora-%OS_VERSION%.repo
      - from-repo: https://copr.fedorainfracloud.org/coprs/sentry/kernel-fsync/repo/fedora-40/sentry-kernel-fsync-fedora-40.repo
        packages:
        - kernel
        - kernel-core
        - kernel-devel
        - kernel-modules
        - kernel-modules-core
        - kernel-modules-extra

  #- type: akmods
  #  base: fsync
  #  install:
  #    - v4l2loopback
      
  #- type: script
  #  scripts:
  #  - fsync-kernel.sh