modules:
  - type: dnf
    repos:
      cleanup: true
      copr:
          - starfish/howdy-beta
    install:
      allow-erasing: true
      install-weak-deps: false
      packages:
      - igt-gpu-tools
      - powertop
      - tuned-utils
      - howdy-gtk
