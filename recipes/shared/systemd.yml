modules:
  - type: systemd
    system:
      enabled:
        - rechunker-group-fix.service
      disabled:
        - NetworkManager-wait-online.service
        - flatpak-add-fedora-repos.service
        - tailscaled.service
    user:
      enabled:
        - topgrade.timer